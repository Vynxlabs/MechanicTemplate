{%- comment -%}
  Text - Core element component
  Renders rich text content with markdown support
{%- endcomment -%}

{%- capture alignClass -%}
  {%- case styles.alignX -%}
    {%- when "center" -%}text-center
    {%- when "end" -%}text-right
    {%- else -%}text-left
  {%- endcase -%}
{%- endcapture -%}

{%- if content.text -%}
  {%- comment -%} Rosey: root element is the tagged one, so _uuid is folded into the key. {%- endcomment -%}
  {%- capture textKey -%}{{ _uuid }}:text{%- endcapture -%}
  <div class="c-text cs-docs cs-docs--{{ color_group | default: 'primary' }} w-full {{ alignClass | strip }}"{{ content.text | roseyMarkdown: textKey }}>
    {%- if env_bookshop_live -%}
      {{ content.text | markdownify }}
    {%- else -%}
      {{ content.text | renderContent | markdownify }}
    {%- endif -%}
  </div>
{%- endif -%}
