{% for accordion in accordions %}    
        
    
<details {% if accordion.opened %}open{% endif %} >

  <summary>
    <span class="details-marker"></span>{% capture accordionKey %}accordion:{{ accordion.title | slug }}{% endcapture %}{{ accordion.title | roseyWrap: accordionKey }}</summary>
  <div class="details-content">
    {% for editorial_block in accordion.editorials %}
        
    {% bookshop {{editorial_block._bookshop_name}} bind: editorial_block %}
    {% endfor %}
  </div>
</details>
{% endfor %}
