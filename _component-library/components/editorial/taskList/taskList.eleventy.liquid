<ul class="task-list-container"{{ _uuid | roseyNs }}>
    {% for task in tasks %}
  <li class="task-list-item"{{ task._uuid | roseyNs }}>
    <p><input
        type="checkbox"
        class="task-list-item-checkbox"
        id="task-item-{{ forloop.index }}"
        {% if task.checked %}
        checked="checked"
        {% endif %}
        disabled="disabled"><label class="task-list-item-label" for="task-item-{{ forloop.index }}">
            {% bookshop "generic/textBlock" text: task.text _uuid: "" roseyKey: "text" %}
        </label>
    </p>
  </li>
    {% endfor %}
</ul>