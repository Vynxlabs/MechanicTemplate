# Metadata about this component, to be used in the CMS
spec:
  structures:
  label: "Language Switcher"
  description: Globe dropdown listing the original language and each locale configured under `languages` in rosey.yml, by native name. Renders nothing unless Rosey is enabled.
  icon: translate
  tags: []

# Defines the structure of this component, as well as the default values
blueprint:
  _componentId: languageSwitcher-0f2a6c1d-9e47-4b28-8c53-6a0d7f1b4e92
  _uuid:

# Overrides any fields in the blueprint when viewing this component in the component browser
preview:

# Any extra CloudCannon inputs configuration to apply to the blueprint
_inputs:
